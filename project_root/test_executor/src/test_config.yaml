# services:
#   backend:
#     unit_test: True 
#     integration_test: False 
#   qdrant_plugin:
#     integration_test: False   
#   tei_plugin:
#     integration_test: False 
#   rdkit_plugin:
#     unit_test: False 
#     integration_test: False 
#   triton_plugin:
#     unit_test: True 
#     integration_test: True 
# jobs: # note job tests require one of tmp_services to have enabled=True
#   hc_job:
#     run_tests: False 
# temp_services:
#   test_consumer:
#     enabled: False
#     run_tests: False
#   test_server:
#     enabled: True
#     run_tests: False

services:
  backend:
    unit_test: True 
    integration_test: True 
  qdrant_plugin:
    integration_test: True   
  tei_plugin:
    integration_test: True 
  rdkit_plugin:
    unit_test: True 
    integration_test: True 
  triton_plugin:
    unit_test: True 
    integration_test: True 
jobs: # note job tests require one of tmp_services to have enabled=True
  hc_job:
    run_tests: True 
temp_services:
  test_consumer:
    enabled: True
    run_tests: True
  test_server:
    enabled: True
    run_tests: True